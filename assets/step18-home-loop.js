/* Carosello infinito: pausa su hover e al clic. */
(function homeInfiniteGallery(){
 function ensureMotion(){document.body?.classList.remove('no-anim');try{localStorage.removeItem('fdc_noanim');localStorage.removeItem('fdcNoAnim')}catch(e){}}
 function build(){ensureMotion();const hero=document.querySelector('#page-home .hero');if(!hero||document.querySelector('.home-loop-gallery')||typeof galleryImages==='undefined')return;const section=document.createElement('section');section.className='home-loop-gallery';section.setAttribute('aria-label','Fotografie della prima edizione');const photos=[...galleryImages,...galleryImages];section.innerHTML=`<div class="home-loop-head"><h2>La prima<br>edizione</h2><p>Un flusso continuo di persone, laboratori e momenti del Festival. Passa sopra le immagini o tocca il carosello per fermarlo.</p></div><div class="home-loop-viewport"><div class="home-loop-track">${photos.map((src,i)=>`<figure class="home-loop-photo"><img src="${src}" loading="${i<3?'eager':'lazy'}" decoding="async" alt="Prima edizione, fotografia ${(i%galleryImages.length)+1}"><figcaption>${String((i%galleryImages.length)+1).padStart(2,'0')} / ${galleryImages.length}</figcaption></figure>`).join('')}</div></div><div class="home-loop-state"></div>`;section.addEventListener('click',()=>section.classList.toggle('paused'));hero.after(section)}
 ensureMotion();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',build,{once:true});else build();setTimeout(ensureMotion,300);setTimeout(ensureMotion,1200);
})();
